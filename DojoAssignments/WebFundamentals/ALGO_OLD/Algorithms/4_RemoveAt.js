// Array: Remove At

// Given an array and an index into array, remove and return the
// array value at that index.
// Do this without using built-in array methods except for
// the pop(). Think of PopFront(arr)
// as equivalent to RemoveAt(arr,0).